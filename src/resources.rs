/// Globally-accessible resources

#[allow(dead_code, reason = "todo")]
#[derive(Default)]
pub enum GameState {
    #[default]
    NotStarted,
    Playing,
    Paused,
}

#[allow(dead_code, reason = "todo")]
pub struct ScoreBoard;
